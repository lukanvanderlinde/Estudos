<script src="/socket.io/socket.io.js"></script>
<script>
  const socket = io('http://localhost:3000');
  socket.on('new-message', function(data) {
    let chat = document.getElementById('chat_' + data.email);
    chat.href += '?sala=' + data.sala;
  });
</script>